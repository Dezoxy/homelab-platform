# Managed by Ansible -- ansible/roles/code_server/templates/code-server.env.j2
# Mode 0600. code-server reads PASSWORD from the environment, so the credential
# never appears in argv (where `ps` would expose it to every user on the box)
# nor in the world-readable systemd unit.
PASSWORD={{ code_server_password }}
