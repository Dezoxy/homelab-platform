# Proxmox host inventory is separate from guest inventory.ini so guest-wide
# maintenance playbooks using `hosts: all` never target the hypervisor.

[proxmox_nodes]
pve ansible_host=192.168.1.239 ansible_user=toomhorvath ansible_ssh_private_key_file=~/.ssh/toomhorvath
